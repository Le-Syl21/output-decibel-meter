# output-decibel-meter
Live loudness metering of what a program actually outputs — per-application or per-device capture on Linux, Windows and macOS, reported as EBU R128 loudness and true peak. Pure Rust, with an optional egui meter.
